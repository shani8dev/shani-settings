#
# $XDG_CONFIG_HOME/psd/psd.conf
#

# Only manage Flatpak Vivaldi — native vivaldi is not installed.
# flatpak_vivaldi is a custom browser definition in ~/.config/psd/browser/
BROWSERS=(flatpak_vivaldi)

USE_BACKUPS="yes"
BACKUP_LIMIT=2
