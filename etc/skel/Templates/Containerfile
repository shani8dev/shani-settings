# -----------------------------------------------------------------------------
# Podman:  podman build -t my-image .
#          podman run -p 8080:8080 my-image
# Docker:  docker build -t my-image .
#          docker run -p 8080:8080 my-image
# Note:    Podman reads this natively. Docker also reads it via
#          the podman-docker compatibility layer.
#          For legacy Docker tooling use Dockerfile instead.
# -----------------------------------------------------------------------------

# Choose your base image
FROM ubuntu:24.04

# Set working directory inside the container
# WORKDIR /home/user

# Install dependencies
# RUN apt-get update && apt-get install -y \
#     package-name \
#     && rm -rf /var/lib/apt/lists/*

# Copy your project files into the container
# COPY . .

# Expose the port your app listens on
# EXPOSE 8080

# Command to run when container starts
# CMD ["./your-app"]
