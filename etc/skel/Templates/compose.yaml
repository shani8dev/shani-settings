# -----------------------------------------------------------------------------
# Podman:  podman-compose up / podman-compose down
# Docker:  docker compose up / docker compose down
# Note:    compose.yaml is the modern standard name, read by both tools.
#          Use this for new projects.
# -----------------------------------------------------------------------------

services:
  app:
    image: my-image
    # build: .
    container_name: my-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - APP_ENV=development
    # env_file:
    #   - .env
    # volumes:
    #   - ./data:/home/user/data
    # depends_on:
    #   - db

  # db:
  #   image: postgres:16
  #   container_name: my-db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: mydb
  #     POSTGRES_USER: myuser
  #     POSTGRES_PASSWORD: mypassword
  #   volumes:
  #     - db-data:/var/lib/postgresql/data

# volumes:
#   db-data:
