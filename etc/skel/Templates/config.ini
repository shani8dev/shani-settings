# -----------------------------------------------------------------------------
# Usage:
#   your-app --config config.ini
#   or place it where your app expects it (check its docs)
# -----------------------------------------------------------------------------

[general]
name = my-app
; debug = false

[server]
; host = localhost
; port = 8080

[database]
; host = localhost
; port = 5432
; name = mydb
