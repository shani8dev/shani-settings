#  SPDX-License-Identifier: LGPL-2.1+
#
#  This file is part of steamos-customizations.
#
#  Copyright © 2022 Valve Corporation.
#
#  steamos-customizations is free software; you can redistribute it and/or
#  modify it under the terms of the GNU Lesser General Public License as
#  published by the Free Software Foundation; either version 2.1 of the License,
#  or (at your option) any later version.

#  Group tasks by session so interactive apps get a fair share of CPU
#  against background batch work (default: 1 — explicitly asserting).
kernel.sched_autogroup_enabled = 1

#  After fork, run the parent first (not the child). Avoids a redundant
#  copy-on-write fault in the child when the parent immediately exec()s
#  (default: 0 — explicitly asserting).
kernel.sched_child_runs_first = 0

#  CFS scheduling slice for bandwidth-throttled cgroups (default: 5000 us).
#  Smaller slice reduces latency for containerised workloads.
kernel.sched_cfs_bandwidth_slice_us = 3000

#  sched_latency_ns, sched_min_granularity_ns, sched_wakeup_granularity_ns, and
#  sched_migration_cost_ns were removed as sysctls when EEVDF replaced CFS as
#  the default scheduler in Linux 6.6 — the underlying tunables either have no
#  successor or moved to a debugfs-only knob (sched_base_slice_ns under
#  /sys/kernel/debug/sched/), which sysctl.d cannot set. They were silent
#  no-ops on this kernel and have been removed rather than kept as dead config.
